const Navbar = () => {
    return (
        <nav className="navbar">
            <h1>EPIC Database</h1>
            <div className="links">
                <a href="/">Home</a>
                <a href="/create">New Purchase</a>

            </div>
        </nav>
    );
};

export default Navbar;

